<UserControl x:Class="test_819.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="1080"
             d:DesignWidth="1920"
            
            
             >
    <Grid Background="ForestGreen">
        <StackPanel Margin="0 10 0 0">
            <TextBlock Text="这是的ViewA" FontSize="100"/>

       
            <TextBlock Text="【发布者】" FontSize="18" FontWeight="Bold"/>
            <TextBox Text="{Binding InputMessage}" Height="30" Margin="0,10,0,10"/>
            <Button Content="发送消息" Width="100" Height="30" Command="{Binding SendCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
