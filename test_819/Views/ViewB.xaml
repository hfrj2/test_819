<UserControl x:Class="test_819.Views.ViewB"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="1080"
             d:DesignWidth="1920">


    
    <Grid Background="Gainsboro">
        <StackPanel Margin="20" Background="LightYellow">
            <TextBlock Text="这是的ViewB" FontSize="100"/>
  
            <TextBlock Text="【订阅者】" FontSize="18" FontWeight="Bold"/>
            <ListBox ItemsSource="{Binding Messages}" 
                     Height="200" 
                     Margin="0,10,0,0"
                     Background="White">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontSize="14" Margin="5"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
